import pandas as pd
import numpy as np
from catboost import CatBoostRegressor
import joblib

# === КОНСТАНТЫ ===
MODEL_PATH = 'energy_forecast_model.cbm'  # Путь к обученной модели
SAMPLE_DATA_PATH = 'sample_prediction_data.csv'  # Пример данных для предсказания

def load_model():
    """Загрузка обученной модели"""
    print("Загрузка модели...")
    model = CatBoostRegressor()
    model.load_model(MODEL_PATH)
    print("Модель загружена успешно")
    return model

def prepare_sample_data():
    """Создание примера данных для предсказания"""
    # Создаем пример данных (в реальности это будут новые данные)
    sample_data = {
        'hour': [14, 15, 16, 17, 18],
        'dayofweek': [1, 1, 1, 1, 1],  # Понедельник
        'month': [3, 3, 3, 3, 3],      # Март
        'temperature': [5.0, 6.0, 7.0, 6.5, 5.5],
        'humidity': [80, 78, 75, 77, 80],
        'wind_speed': [3.0, 3.5, 4.0, 3.5, 3.0],
        'consumption_lag_1': [1200, 1250, 1300, 1280, 1260],
        'consumption_lag_2': [1180, 1200, 1250, 1300, 1280],
        'consumption_lag_3': [1150, 1180, 1200, 1250, 1300],
        'consumption_lag_24': [1100, 1120, 1150, 1180, 1200],
        'consumption_lag_48': [1080, 1100, 1120, 1150, 1180],
        'consumption_lag_168': [1150, 1160, 1180, 1200, 1220],
        'consumption_rolling_mean_3': [1170, 1210, 1250, 1270, 1280],
        'consumption_rolling_mean_6': [1150, 1180, 1210, 1240, 1260],
        'consumption_rolling_mean_12': [1120, 1150, 1180, 1210, 1230],
        'consumption_rolling_mean_24': [1100, 1120, 1150, 1180, 1200],
        'consumption_rolling_std_3': [50, 55, 60, 58, 55],
        'consumption_rolling_std_6': [45, 50, 55, 53, 50],
        'consumption_rolling_std_12': [40, 45, 50, 48, 45],
        'consumption_rolling_std_24': [35, 40, 45, 43, 40],
        'is_holiday': [0, 0, 0, 0, 0],
        'is_working_weekend': [0, 0, 0, 0, 0],
        'is_regular_weekend': [0, 0, 0, 0, 0],
        'is_working_day': [1, 1, 1, 1, 1],
        'is_weekend_or_holiday': [0, 0, 0, 0, 0],
        'is_weekend': [0, 0, 0, 0, 0]
    }
    
    df_sample = pd.DataFrame(sample_data)
    df_sample.to_csv(SAMPLE_DATA_PATH, index=False)
    print(f"Пример данных сохранен в {SAMPLE_DATA_PATH}")
    return df_sample

def make_predictions():
    """Выполнение предсказаний"""
    print("=== ВЫПОЛНЕНИЕ ПРЕДСКАЗАНИЙ ===")
    
    # Загрузка модели
    model = load_model()
    
    # Подготовка данных для предсказания
    df_sample = prepare_sample_data()
    
    # Выполнение предсказаний
    predictions = model.predict(df_sample)
    
    # Вывод результатов
    print("\n=== РЕЗУЛЬТАТЫ ПРЕДСКАЗАНИЙ ===")
    for i, pred in enumerate(predictions):
        print(f"Час {df_sample.iloc[i]['hour']}:00 - {pred:.1f} МВт")
    
    # Сохранение результатов
    results_df = df_sample.copy()
    results_df['predicted_consumption'] = predictions
    results_df.to_csv('predictions_results.csv', index=False)
    print("\nРезультаты сохранены в predictions_results.csv")
    
    return predictions

def explain_model():
    """Объяснение принципов работы модели"""
    print("\n" + "="*60)
    print("ИНФОРМАЦИЯ О МОДЕЛИ И ПРЕДСКАЗАНИЯХ")
    print("="*60)
    
    print("""
🎯 ЧТО ИСПОЛЬЗУЕТСЯ ДЛЯ ПРЕДСКАЗАНИЙ:

🔢 ЧИСЛОВЫЕ ПРИЗНАКИ:
• hour (0-23) - Час суток
• dayofweek (0-6) - День недели
• month (1-12) - Месяц
• temperature - Температура (°C)
• humidity - Влажность (%)
• wind_speed - Скорость ветра (м/с)
• consumption_lag_* - Потребление в прошлом
• consumption_rolling_* - Скользящие средние

📋 КАТЕГОРИАЛЬНЫЕ ПРИЗНАКИ:
• is_holiday - Праздничный день
• is_working_day - Рабочий день
• is_weekend - Выходной день
• is_weekend_or_holiday - Выходной или праздник

🧠 ПРИНЦИП РАБОТЫ МОДЕЛИ:
1. CatBoost строит ансамбль деревьев решений
2. Каждое дерево учится на ошибках предыдущих
3. Автоматически обрабатывает категориальные признаки
4. Устойчив к выбросам и пропускам

📥 ЧТО ОЖИДАЕТСЯ НА ВХОД:
DataFrame с колонками, соответствующими признакам
Все числовые значения должны быть заполнены
Категориальные признаки в формате 0/1

📈 ТОЧНОСТЬ МОДЕЛИ:
Цель: MAE < 2.5% от среднего значения потребления
Используется метрика MAE (средняя абсолютная ошибка)
""")
    
    print("="*60)

def main():
    """Основная функция тестирования"""
    try:
        explain_model()
        predictions = make_predictions()
        print("\n=== ТЕСТИРОВАНИЕ ЗАВЕРШЕНО ===")
    except Exception as e:
        print(f"Ошибка при тестировании: {e}")

if __name__ == "__main__":
    main()
